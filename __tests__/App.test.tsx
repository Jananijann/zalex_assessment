/**
 * App-level smoke test is optional per CLAUDE_CONTEXT.md.
 * The required tests (validation, filters, purpose edit) are in src/tests/.
 */
it('placeholder', () => {
  expect(true).toBe(true);
});
